#include<stdio.h>

int main(){
    // Pointer Arithamatic using integer pointer
    // int a = 5;
    // int *ptr = &a;
    // printf("The address of a is %u \n", &a);
    // printf("the address of a is %u \n", ptr);
    // ptr++;
    // printf("the address of a is %u \n", ptr);

    // Pointer Arithamatic using char pointer
    char a = 'A';
    char *ptr = &a;
    printf("The address of a is %u \n", &a);
    printf("the address of a is %u \n", ptr);
    ptr++;
    printf("the address of a is %u \n", ptr);


    
    

}